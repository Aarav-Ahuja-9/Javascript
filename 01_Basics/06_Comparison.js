// Comparision in JS

// console.log(2 > 1);
// console.log(2 >= 1);
// console.log(2 < 1);
// console.log(2 == 1);
// console.log(2 != 1);

// They are so simple thats why we are just touching it 

// When it becomes tricky is when you dont convert same data type like this :-

// console.log("2" > 1) // javascript will automatcally convert the datatype and the answer will be "true"
// console.log("02" > 1) // this is the same and it is true


// let's check if we can compare null with 0 😊😂

// console.log(null > 0) // the answer will be "false"
// console.log(null == 0) // will be "false"
// console.log(null >= 0) // Will be "true"

// Let's check if we can compare undefined with 0

// console.log(undefined > 0) // False
// console.log(undefined == 0) // False
// console.log(undefined >= 0) // False
// console.log(undefined === 0) // False

// In this case if you use any operator the answer will be still false

